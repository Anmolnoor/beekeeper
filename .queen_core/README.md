# Queen Core Canonical Config

- Source of truth for editable soul/context/worker-registry/prompt files.
- Runtime compatibility is preserved via symlinks from `.honeycomb/...` and root `SOUL.md`/`.soul.json`.
- Update files in `.queen_core` only; linked runtime paths reflect those changes.
